# Python bindings

Python will provide scientific exploration and model-extension surfaces through
PyO3/maturin. It must call the authoritative Rust implementation and must not
become a second production dose or QA engine.

Bindings are deferred until the R1 geometry contract stabilizes.
