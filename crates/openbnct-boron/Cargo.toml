[package]
name = "openbnct-boron"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "PET-derived boron uptake models and field artifacts for NCTForge"

[dependencies]
openbnct-core = { path = "../openbnct-core" }
openbnct-transport = { path = "../openbnct-transport" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
